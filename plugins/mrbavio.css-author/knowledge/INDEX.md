# Knowledge index

Generated by `pnpm knowledge:index` from each file's frontmatter — do not edit by hand. Read a file with the MCP `knowledge_read` tool by the path in parentheses, or search with `knowledge_search`.

## Procedures — read first, every time

- procedure · procedures — Decision procedures against the three judgment failures — which formatting context, whether a declaration is needed at all, where in the cascade to set it — and the author → land → fix → land loop. (procedures.md)

## Examples — intent → minimal document

- example · three-column-page — A navigation, article and aside as one grid — fixed rem sidebars, a fluid fr article — that stacks into one column below 640px through a single media rule on the body; the two sidebars share one .card rule. (examples/three-column-page.md)

## Reference — read on demand

- reference · formatting-contexts — What a formatting context is, which declarations establish one (block, inline, flex, grid), what establishing one changes for the children, and how the containing block for positioned boxes is chosen. (reference/formatting-contexts.md)
