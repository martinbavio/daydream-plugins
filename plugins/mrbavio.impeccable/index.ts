// mrbavio.impeccable — Impeccable's design verbs (impeccable.style) as MCP
// prompts over a viewport on the canvas. The whole of the first slice is
// the HOST PART (bridge.ts): the browser part registers nothing yet. It
// exists so the plugin has a tile on the plugins page and a switch; the
// adopt-variant command (a variant draft's payload into its source
// viewport, one undo step) lands here in a later slice.

import type { DaydreamApi } from "@daydream/plugin-api";

export default function activate(_dd: DaydreamApi): void {}
